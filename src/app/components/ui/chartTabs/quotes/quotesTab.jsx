import { TableCard } from "../../../common/card";

const QuotesTab = ({ md, title, bodyItems }) => {
  return (
    <TableCard
      md={md}
      title={title}
      body={bodyItems}
      //
    />
  );
};

export default QuotesTab;
